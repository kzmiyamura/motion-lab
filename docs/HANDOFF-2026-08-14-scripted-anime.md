# 引継ぎ 2026-08-14 — 方針転換: 手描きアニメ（キーポーズ方式）

## 経緯（重要）

ユーザーは動画復元リグの見た目を「こわれた人形が絡み合ってる」と却下。
**動画データを使わず、きれいなアニメを先に完成させる**方針に転換した。
「漫画のように一枚一枚丁寧に描く」= キーポーズ方式。
**解析対象の動画は今後ほぼ On2 になる予定**（ユーザー談）。

## 現状（全部 main に push 済み・テスト50本green）

- `src/engine/scriptedClip.ts` — 手描き合成クリップ（MotionClip 形式）。
  UI は 3D タブの「✍️ ベーシック On1 / ✍️ ベーシック On2 / ✍️ 手描きCBL」ボタン
- `CoupleFigure.tsx` — keyPose 方式（既定ON、`?keyPose=0` で旧観測追従）。
  scripted クリップ（`armTimeline.source==='scripted'`）の足首は振付の正解として踏む
- ベーシック足運びは **ユーザー2回の添削を経て確定**。仕様は memory
  `salsa-basic-footwork-rules` とテスト `src/__tests__/scriptedClip.test.ts` に固定:
  - カウント1に動くのは男左足・女右足（On1/On2共通）
  - On1: 1前ブレイク→3戻す→5後ろ→7戻す（3478ニュートラル）
  - On2: 位置が変わる歩は**1,2,5,6だけ**。3は1と同じ場所・7は5と同じ場所で踏み直し。
    8ANDから左足→1で右足の少し後ろ、2は1の後ろ（ブレイク）、4ANDから右足→5で前、
    6はさらに前（ブレイク）。**両足は一度も揃わない**
- **On2 の最新配置（1,2,5,6 + 踏み直し）はコミット済みだがユーザー未確認**。
  次セッション最初にユーザーに On2 を見てもらうこと

## 次の一手

1. ユーザーに On2 ベーシックの目視確認をもらう（0.25倍スローで足元）
2. OK なら CBL（`buildScriptedCBL`）の足運びを同じ流儀で On2 前提に作り直す
   （現状 CBL の足は手続きの揺れのまま = 未着手。5-6-7 のトラベルを足キーで書く）
3. その先: 動画復元側も On2 前提（ブレイク2・6）にビート位相・armTimeline を寄せる

## 環境メモ

- vite dev（5173）と pm2 motion-lab-server（4000）は**起動したまま**。
  RAM が逼迫したら `pm2 stop motion-lab-server` と vite の子プロセス kill
  （memory `pm2-port-4000-conflict` 参照）
- 動作確認は Chrome タブで 3D タブ → ✍️ ボタン。`__clipSeek(秒)` でシーク、
  一時停止は「⏸ 停止」ボタン。カメラは「正面」がスロットを横から見る向き
- 実装順序4（前回引継ぎ `HANDOFF-2026-08-13-hold-verification.md`）の
  「技5例の目視確認」は**保留のまま**。手描きアニメ路線が優先
